services:
  backend:
    build:
      context: ./backend
    container_name: chat-widget-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/data
      - ./backend/logs:/app/logs
      - ./widget:/app/widget
      - ./demo:/app/demo
